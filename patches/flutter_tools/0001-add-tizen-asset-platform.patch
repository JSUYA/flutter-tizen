diff --git a/packages/flutter_tools/lib/src/asset.dart b/packages/flutter_tools/lib/src/asset.dart
index 2f13da2ef01..1a44f2ef783 100644
--- a/packages/flutter_tools/lib/src/asset.dart
+++ b/packages/flutter_tools/lib/src/asset.dart
@@ -198,0 +199 @@ abstract class AssetBundle {
+    String? assetPlatform,
@@ -329,0 +331 @@ class ManifestAssetBundle implements AssetBundle {
+    String? assetPlatform,
@@ -389,0 +392 @@ class ManifestAssetBundle implements AssetBundle {
+      assetPlatform: assetPlatform,
@@ -405,0 +409 @@ class ManifestAssetBundle implements AssetBundle {
+          assetPlatform: assetPlatform,
@@ -484,0 +489 @@ class ManifestAssetBundle implements AssetBundle {
+          assetPlatform: assetPlatform,
@@ -842,0 +848 @@ class ManifestAssetBundle implements AssetBundle {
+    String? assetPlatform,
@@ -884 +890,2 @@ class ManifestAssetBundle implements AssetBundle {
-            !asset.matchesFlavor(flavor) || !asset.matchesPlatform(targetPlatform),
+            !asset.matchesFlavor(flavor) ||
+            !asset.matchesPlatform(targetPlatform, assetPlatform: assetPlatform),
@@ -1026,0 +1034 @@ class ManifestAssetBundle implements AssetBundle {
+    String? assetPlatform,
@@ -1076 +1084 @@ class ManifestAssetBundle implements AssetBundle {
-      if (!asset.matchesPlatform(targetPlatform)) {
+      if (!asset.matchesPlatform(targetPlatform, assetPlatform: assetPlatform)) {
@@ -1081 +1089 @@ class ManifestAssetBundle implements AssetBundle {
-          'Target platform: ${targetPlatform.osName}\n',
+          'Target platform: ${assetPlatform ?? targetPlatform.osName}\n',
@@ -1507 +1515 @@ class _Asset {
-  bool matchesPlatform(TargetPlatform targetPlatform) {
+  bool matchesPlatform(TargetPlatform targetPlatform, {String? assetPlatform}) {
@@ -1512 +1520 @@ class _Asset {
-    return platforms.contains(targetPlatform.osName);
+    return platforms.contains(assetPlatform ?? targetPlatform.osName);
diff --git a/packages/flutter_tools/lib/src/build_system/targets/assets.dart b/packages/flutter_tools/lib/src/build_system/targets/assets.dart
index f9ebbae2045..b8e6ae73357 100644
--- a/packages/flutter_tools/lib/src/build_system/targets/assets.dart
+++ b/packages/flutter_tools/lib/src/build_system/targets/assets.dart
@@ -39,0 +40 @@ Future<Depfile> copyAssets(
+  String? assetPlatform,
@@ -57,0 +59 @@ Future<Depfile> copyAssets(
+    assetPlatform: assetPlatform,
diff --git a/packages/flutter_tools/lib/src/flutter_manifest.dart b/packages/flutter_tools/lib/src/flutter_manifest.dart
index a33f2ae3ca7..56bab017a3d 100644
--- a/packages/flutter_tools/lib/src/flutter_manifest.dart
+++ b/packages/flutter_tools/lib/src/flutter_manifest.dart
@@ -21,0 +22 @@ const _kValidPluginPlatforms = <String>{'android', 'ios', 'web', 'windows', 'lin
+const _kValidAssetPlatforms = <String>{..._kValidPluginPlatforms, 'tizen'};
@@ -954 +955 @@ class AssetsEntry {
-      final Set<String> invalidPlatforms = platforms.toSet().difference(_kValidPluginPlatforms);
+      final Set<String> invalidPlatforms = platforms.toSet().difference(_kValidAssetPlatforms);
@@ -960 +961 @@ class AssetsEntry {
-            'Invalid platform(s): "${invalidPlatforms.join(", ")}". Supported platforms are: "${_kValidPluginPlatforms.join(", ")}".',
+            'Invalid platform(s): "${invalidPlatforms.join(", ")}". Supported platforms are: "${_kValidAssetPlatforms.join(", ")}".',
diff --git a/packages/flutter_tools/test/general.shard/build_system/targets/assets_test.dart b/packages/flutter_tools/test/general.shard/build_system/targets/assets_test.dart
index cf55efed9e3..c14bafaa979 100644
--- a/packages/flutter_tools/test/general.shard/build_system/targets/assets_test.dart
+++ b/packages/flutter_tools/test/general.shard/build_system/targets/assets_test.dart
@@ -21,0 +22 @@ import 'package:flutter_tools/src/globals.dart' as globals;
+import 'package:flutter_tools/src/isolated/native_assets/dart_hook_result.dart';
@@ -616,0 +618,43 @@ flutter:
+
+    testUsingContext(
+      'uses assetPlatform override when filtering assets',
+      () async {
+        fileSystem.file('pubspec.yaml')
+          ..createSync()
+          ..writeAsStringSync('''
+name: example
+flutter:
+  assets:
+    - path: assets/common.txt
+    - path: assets/tizen.txt
+      platforms:
+        - tizen
+    - path: assets/android.txt
+      platforms:
+        - android
+''');
+        writePackageConfigFiles(directory: globals.fs.currentDirectory, mainLibName: 'example');
+        fileSystem.file('assets/common.txt').createSync(recursive: true);
+        fileSystem.file('assets/tizen.txt').createSync(recursive: true);
+        fileSystem.file('assets/android.txt').createSync(recursive: true);
+
+        final Directory output = environment.buildDir.childDirectory('flutter_assets')
+          ..createSync(recursive: true);
+        await copyAssets(
+          environment,
+          output,
+          dartHookResult: DartHooksResult.empty(),
+          targetPlatform: TargetPlatform.android,
+          assetPlatform: 'tizen',
+          buildMode: BuildMode.debug,
+        );
+
+        expect(output.childFile('assets/common.txt'), exists);
+        expect(output.childFile('assets/tizen.txt'), exists);
+        expect(output.childFile('assets/android.txt'), isNot(exists));
+      },
+      overrides: <Type, Generator>{
+        FileSystem: () => fileSystem,
+        ProcessManager: () => FakeProcessManager.any(),
+      },
+    );
diff --git a/packages/flutter_tools/test/general.shard/devfs_test.dart b/packages/flutter_tools/test/general.shard/devfs_test.dart
index ba5e9858395..d4498ebab21 100644
--- a/packages/flutter_tools/test/general.shard/devfs_test.dart
+++ b/packages/flutter_tools/test/general.shard/devfs_test.dart
@@ -986 +986 @@ class FakeBundle extends AssetBundle {
-    String? packageConfigPath,
+    required String packageConfigPath,
@@ -988 +988,2 @@ class FakeBundle extends AssetBundle {
-    TargetPlatform? targetPlatform,
+    required TargetPlatform targetPlatform,
+    String? assetPlatform,
diff --git a/packages/flutter_tools/test/general.shard/flutter_manifest_test.dart b/packages/flutter_tools/test/general.shard/flutter_manifest_test.dart
index a243df8a77c..9689deefc7c 100644
--- a/packages/flutter_tools/test/general.shard/flutter_manifest_test.dart
+++ b/packages/flutter_tools/test/general.shard/flutter_manifest_test.dart
@@ -1588,0 +1589 @@ flutter:
+        - tizen
@@ -1599 +1600 @@ flutter:
-    expect(entry.platforms, containsAll(<String>['web', 'android']));
+    expect(entry.platforms, containsAll(<String>['web', 'android', 'tizen']));
